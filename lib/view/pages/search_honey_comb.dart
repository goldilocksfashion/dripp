import 'package:flutter/material.dart';

/// TODO: Implement using HoneyComb Layout
class SearchPageWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Search Honey Comb'),
      ),
      body: Center(
        child: Text('Search Honey Comb'),
      ),
    );
  }
}
